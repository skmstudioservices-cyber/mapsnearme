name: Pages Inspect

on:
  workflow_dispatch:

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: full sitemap source
        run: |
          set -e
          sed -n '30,140p' src/pages/sitemap.xml.ts
